# 🌳 Git 브랜치 및 태그 관리 요약

---

## 1. 브랜치(Branch) 개요 및 관리

### 1.1 브랜치 개요 및 필요성
* [cite_start]**정의**: 버전 관리를 수행하던 일련의 파일 집합을 통째로 복사해 **독립적으로 다시 개발을 진행하는 개념**입니다[cite: 1949].
* [cite_start]**필요성**: 여러 개발자가 타인을 신경 쓰지 않고 동시에 다양한 작업을 할 수 있게 만들어 주는 기능입니다[cite: 1950].
* [cite_start]**장점**: 저장소에서 다른 브랜치에 영향 없이 새로운 기능 개발, 버그 수정, 새로운 아이디어 실험이 가능합니다[cite: 1959, 1960, 1961].
* [cite_start]**브랜치 병합(`merge`)**: 독립된 브랜치에서 작업한 후, 원래 버전과 합치는 수행입니다[cite: 1953, 1963].

### 1.2 브랜치와 HEAD 포인터
* [cite_start]**브랜치**: 커밋 사이를 가볍게 이동할 수 있는 포인터입니다[cite: 2003].
* [cite_start]**HEAD**: **현재 작업 중인 브랜치**의 최신 커밋을 가리키는 포인터입니다[cite: 2005].
    * [cite_start]**`HEAD -> main`**: 'main'이 마지막 커밋을 가리키고, HEAD는 현재 작업 브랜치인 'main'을 가리킨다[cite: 2010, 2011].
* [cite_start]**분리된 HEAD (`detached HEAD`)**: HEAD가 현재 브랜치(마지막 커밋)가 아닌 **그 이전 커밋을 가리키는 상태**를 의미합니다[cite: 977, 2125].

### 1.3 브랜치 생성 및 이동 명령
Git은 `$ git checkout`과 새로운 `$ git switch` 명령을 모두 지원합니다.

| 작업 내용 | `git branch` (생성만) | `git checkout` (생성 및 이동) | `git switch` (생성 및 이동) |
| :--- | :--- | :--- | :--- |
| **목록 보기** | [cite_start]`$ git branch` [cite: 1307, 2347] | - | - |
| **생성만** | [cite_start]`$ git branch <bname>` [cite: 890, 2349] | - | - |
| **생성 후 이동** | - | [cite_start]`$ git checkout -b <bname>` [cite: 893, 2351] | [cite_start]`$ git switch -c <bname>` [cite: 892, 2353] |
| **지정 브랜치로 이동** | - | [cite_start]`$ git checkout <bname>` [cite: 951, 2321] | [cite_start]`$ git switch <bname>` [cite: 946, 2323] |
| **이전 브랜치로 이동** | - | [cite_start]`$ git checkout -` [cite: 963, 2325] | [cite_start]`$ git switch -` [cite: 957, 2337] |
| **이전 커밋으로 이동** (Detached HEAD) | - | [cite_start]`$ git checkout HEAD~` [cite: 2341] | [cite_start]`$ git switch -d HEAD~` [cite: 981, 2341] |

### 1.4 브랜치 삭제 명령
* **`$ git branch -d <bname>`**: 지정한 브랜치 이름을 삭제합니다. [cite_start]**이미 병합된** 브랜치만 삭제 가능합니다[cite: 1314, 2262].
* [cite_start]**`$ git branch -D <bname>`**: 병합 여부와 관계없이 **강제로** 브랜치를 삭제합니다[cite: 1316, 2262].
* [cite_start]**삭제 조건**: 일반적으로 현재 작업 중인 브랜치(HEAD)는 삭제할 수 없습니다[cite: 2280].

---

## 2. 버전과 태그(Tag) 활용

### 2.1 버전 관리와 SemVer 방식
* [cite_start]**버전(version)**: 프로그램을 수정하거나 개선할 때 코드를 구분하려고 부여된 식별자입니다[cite: 3025].
* [cite_start]**SemVer (Semantic Versioning)**: `$ major.minor.patch` 형태의 세 자리 숫자 버전 표기 방식입니다[cite: 3041, 3042].
    * [cite_start]**Major (메이저)**: 정식 버전은 1부터 시작하며, 큰 기능 추가/변경 시 바뀝니다[cite: 3045, 3047].
    * [cite_start]**Minor (마이너)**: 기능을 추가하거나 변경 사항이 있을 때 바꿉니다[cite: 3047].
    * [cite_start]**Patch (패치)**: 버그 수정 등 미미한 변화가 있을 때 바꿉니다[cite: 3049].

### 2.2 태그(Tag) 기능
* [cite_start]**정의**: 특정 커밋(해시 값)에 버전 번호나 다른 이름을 부여하는 기능입니다[cite: 3059].
* **종류**:
    * [cite_start]**주석 태그 (Annotated tag)**: 태그 이름과 함께 태그 작성자, 시각, 태그 메시지 등 정보를 포함합니다[cite: 3062, 3063, 3083].
    * [cite_start]**일반/가벼운 태그 (Lightweight tag)**: 태그 이름만 포함합니다[cite: 3064, 3065].

### 2.3 태그 명령
| 명령어 | 종류 | 설명 |
| :--- | :--- | :--- |
| **`$ git tag v1.0.1`** | 일반 태그 | [cite_start]현재 커밋에 태그 이름만 붙입니다[cite: 3104]. |
| **`$ git tag -a v1.0.0 -m 'msg'`** | 주석 태그 | [cite_start]현재 커밋에 메시지(`-m`)를 포함한 주석 태그를 생성합니다[cite: 3085, 3193]. |
| **`$ git tag -a v1.1.0 HEAD~`** | 주석 태그 | [cite_start]**특정 커밋**(`HEAD~`)에 태그를 붙입니다[cite: 3092, 3095]. |
| **`$ git tag`** | - | [cite_start]생성된 태그 목록을 표시합니다[cite: 3115, 3197]. |
| **`$ git show <tagname>`** | - | [cite_start]주석 태그의 경우 **태그 정보와 커밋 정보**를 자세히 표시합니다[cite: 3129, 3195]. |
| **`$ git tag -d <tagname>`** | - | [cite_start]지정한 태그를 삭제합니다[cite: 3155, 3199]. |